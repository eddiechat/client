[package]
name    = "email_classifier"
version = "0.1.0"
edition = "2021"

[lib]
name       = "email_classifier"
crate-type = ["rlib"]

[dependencies]
serde_json = "1"
regex      = "1"
once_cell  = "1"
